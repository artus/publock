import { Publock } from './Publock';

function functionalityWorks(message : string, works : boolean)
{
    
}

function creating_a_new_publock_works()
{
    let newPublock = new Publock();
    
    // Check if all the properties have been initialised
}